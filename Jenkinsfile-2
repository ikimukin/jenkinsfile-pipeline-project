pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Re-invent Yourself'
                sh 'python3 --version'
                sh 'python3 pipeline.py'
            }
        }
    }
}